name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: https://vcarfmhherrkevzziqdq.supabase.co
      NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZjYXJmbWhoZXJya2V2enppcWRxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE3MjAzMDQsImV4cCI6MjA5NzI5NjMwNH0.8uck-z7J6En8IO52e41aTKj3VnUCaUO4QRIOkhJKomg
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Lint (informativo, no bloquea el build)
        run: npm run lint
        continue-on-error: true

      - run: npm run build
